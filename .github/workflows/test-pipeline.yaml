name: "test pipeline"

# on:
#   pull_request_target:
#     types:
#       - opened
#       - edited
#       - synchronize

on:
  push:
    branches:
      - "*"

permissions:
  pull-requests: write

jobs:
  compliance:
    uses: open-sauced/open-sauced/.github/workflows/compliance.yml@main
